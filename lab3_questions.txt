5.
8.5.1 datasheet

A tick is a periodic signal which provides a timebase for a timer or counter

1 micro s

6.

8.5.1 datasheet
"The tick generators use clk_ref as their reference clock"

7.
12.8.3 2nd line
"they can be fired at a maximum of 2^32 microseconds into the future."

8.
12.8.3

To enable an alarm:
	1. Enable the interrupt at the timer with a write to the appropriate alarm bit in INTE (e.g. (1 << 0) for ALARM0).
	2. Enable the appropriate timer interrupt at the processor (see Section 3.2).
	3. Write the time you would like the interrupt to fire to ALARM0 (i.e. the current value in TIMERAWL plus your desired alarm
	time in microseconds). Writing the time to the ALARM register sets the ARMED bit as a side effect.
	
SDK pg. 92
//sets bits of the &timer_hw->inte register to whatever mask you want
//&timer_hw->inte
hw_set_bits (&timer_hw->inte, uint32_t mask);

9.

12.8.4.2 example
hw_clear_bits(&timer_hw->intr, 1u << ALARM_NUM);

